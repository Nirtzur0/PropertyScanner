# llm.yaml
# LiteLLM configuration (ordered fallback list).

llm:
  models:
    - "ollama/llama3:latest"
    - "gemini/gemini-1.5-flash"
    - "gpt-4o-mini"
  temperature: 0.0
  max_tokens: 900
  timeout_seconds: 60
  normalizer_enabled: false
  normalizer_max_chars: 6000
